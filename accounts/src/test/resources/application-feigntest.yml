spring:
  config:
    import: "optional:configserver:"
  cloud:
    discovery:
      enabled: false
    kubernetes:
      discovery:
        enabled: false
    openfeign:
      circuitbreaker:
        enabled: true
